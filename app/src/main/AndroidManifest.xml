<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.tecnm.tepic.ladm_tarea1_muchosactivitys">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LADM_Tarea1_MuchosActivitys">
        <activity
            android:name=".TerActivityEmp"
            android:exported="false" />
        <activity
            android:name=".SegActivityBasic"
            android:exported="false"
            android:label="@string/title_activity_seg_basic"
            android:theme="@style/Theme.LADM_Tarea1_MuchosActivitys.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>